<Window x:Class="KIP2.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KIP2.Views"
        xmlns:localViewModels="clr-namespace:KIP2.ViewModels"
        mc:Ignorable="d"
		d:DataContext="{d:DesignInstance localViewModels:MainWindowViewModel, IsDesignTimeCreatable=True}"
		SizeToContent="WidthAndHeight"
        Title="Kinect Image Processor">

	<Window.DataContext>
		<localViewModels:MainWindowViewModel />
	</Window.DataContext>

	<Window.Resources>
		<DataTemplate DataType="{x:Type localViewModels:VisualSensorViewModel}">
			<local:VisualSensorControl />
		</DataTemplate>
	</Window.Resources>

	<DockPanel>
		<StatusBar DockPanel.Dock="Bottom">
			<StatusBar.ItemsPanel>
				<ItemsPanelTemplate>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>
					</Grid>
				</ItemsPanelTemplate>
			</StatusBar.ItemsPanel>

			<StatusBarItem Grid.Column="0">
				<TextBlock Text="{Binding StatusText}" />
			</StatusBarItem>
		</StatusBar>

		<ContentControl Margin="10" Content="{Binding ContentViewModel}" />
	</DockPanel>
</Window>